# 安全策略

## 支持的版本

| 版本 | 支持状态       |
|------|------------|
| 1.x  | ✅ 持续支持中   |

## 报告漏洞

如果你发现安全漏洞，**请不要提公开 Issue**。

请发送邮件至 **guan.x.l@icloud.com** 报告漏洞。

请在报告中包含以下信息：

- 漏洞的详细描述
- 复现步骤
- 影响范围评估
- 可能的修复建议（如有）

### 我们的承诺

- 收到报告后 **7 天内** 确认收到
- 在 **30 天内** 提供修复方案
- 在修复发布前对漏洞细节保密
- 在修复发布后公开致谢报告者（需经报告者同意）

## 安全最佳实践

部署 GauzyNote 时请注意：

1. **修改默认密码**：部署后立即修改 `admin` 账号的默认密码
2. **关闭 SQL 初始化**：生产环境设置 `spring.sql.init.mode=NEVER`
3. **配置 CORS**：限制 `app.cors.allowed-origins` 为你的实际域名
4. **使用 HTTPS**：通过 Nginx 配置 SSL 证书
5. **设置 Redis 密码**：生产环境务必设置 Redis 认证密码
6. **定期备份数据库**
