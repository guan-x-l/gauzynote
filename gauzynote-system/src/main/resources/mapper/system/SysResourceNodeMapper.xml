<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gauzynote.system.mapper.SysResourceNodeMapper">

    <resultMap type="com.gauzynote.system.domain.entity.SysResourceNode" id="SysResourceNodeMap">
        <result property="nodeId" column="node_id" jdbcType="INTEGER"/>
        <result property="parentId" column="parent_id" jdbcType="INTEGER"/>
        <result property="relatedId" column="related_id" jdbcType="INTEGER"/>
        <result property="userId" column="user_id" jdbcType="INTEGER"/>
        <result property="nodeType" column="node_type" jdbcType="VARCHAR"/>
        <result property="nodeName" column="node_name" jdbcType="VARCHAR"/>
        <result property="sort" column="sort" jdbcType="INTEGER"/>
        <result property="nodePath" column="node_path" jdbcType="VARCHAR"/>
        <result property="depth" column="depth" jdbcType="INTEGER"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="selectById" resultMap="SysResourceNodeMap">
        select
        *
        from sys_resource_node
        where node_id = #{nodeId}
          and del_flag = '0'
    </select>

    <!--查询多个-->
    <select id="selectByIds" resultMap="SysResourceNodeMap">
        select
        *
        from sys_resource_node
        where node_id in
        <foreach collection="list" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          and del_flag = '0'
    </select>


    <!--查询单个-->
    <select id="selectByRelatedId" resultMap="SysResourceNodeMap">
        select
        *
        from sys_resource_node
        where related_id = #{relatedId}
          and del_flag = '0'
    </select>
    <!--查询单个-->
    <select id="selectByParentIdAndNodeName" resultMap="SysResourceNodeMap">
        select
        *
        from sys_resource_node
        where user_id = #{userId}
            <if test="parentId != null">and parent_id = #{parentId}</if>
            <if test="parentId == null">and parent_id is null </if>
            and node_type = #{nodeType}
            and node_name = #{nodeName}
            and del_flag = '0'
    </select>

    <!--查询-->
    <select id="selectAllByUserId" resultMap="SysResourceNodeMap">
        select
        *
        from sys_resource_node
        where user_id = #{userId}
          and del_flag = '0'
    </select>
    <!--查询-->
    <select id="selectAllChildrenByNodeId" resultMap="SysResourceNodeMap">
        SELECT * FROM sys_resource_node
        WHERE (node_path LIKE CONCAT('%/', #{nodeId}, '/%') OR node_id = #{nodeId})
          AND del_flag = '0'
    </select>
    <!--查询asset-->
    <select id="selectAssetsByNodeNameAndDepth" resultMap="SysResourceNodeMap">
        SELECT *
        FROM sys_resource_node
        WHERE user_id = #{userId}
            and node_type = '1'
            and node_name = #{nodeName}
            and depth = #{depth}
            and del_flag = '0'
            and parent_id IS NULL
    </select>

    <!--新增-->
    <insert id="insert" keyProperty="nodeId" useGeneratedKeys="true">
        insert into sys_resource_node(
        <if test="parentId != null and parentId != ''">parent_id,</if>
        <if test="relatedId != null and relatedId != ''">related_id,</if>
        <if test="userId != null and userId != ''">user_id,</if>
        <if test="nodeType != null and nodeType != ''">node_type,</if>
        <if test="nodeName != null and nodeName != ''">node_name,</if>
        <if test="sort != null and sort != ''">sort,</if>
        <if test="nodePath != null and nodePath != ''">node_path,</if>
        <if test="depth != null and depth != ''">depth,</if>
        create_time
        )values(
        <if test="parentId != null and parentId != ''">#{parentId},</if>
        <if test="relatedId != null and relatedId != ''">#{relatedId},</if>
        <if test="userId != null and userId != ''">#{userId},</if>
        <if test="nodeType != null and nodeType != ''">#{nodeType},</if>
        <if test="nodeName != null and nodeName != ''">#{nodeName},</if>
        <if test="sort != null and sort != ''">#{sort},</if>
        <if test="nodePath != null and nodePath != ''">#{nodePath},</if>
        <if test="depth != null and depth != ''">#{depth},</if>
        sysdate()
        )
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update sys_resource_node
        <set>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="relatedId != null">related_id = #{relatedId},</if>
            <if test="nodeName != null and nodeName != ''">node_name = #{nodeName},</if>
            <if test="sort != null">sort = #{sort},</if>
            <if test="nodePath != null and nodePath != ''">`node_path` = #{nodePath},</if>
            <if test="depth != null">depth = #{depth},</if>
            update_time = sysdate()
        </set>
        where node_id = #{nodeId} and user_id = #{userId} and del_flag = '0'
    </update>

    <update id="updateNodeName">
        update sys_resource_node SET node_name = #{nodeName}, update_time = sysdate() where node_id = #{nodeId} and user_id = #{userId} and del_flag = '0'
    </update>
    <update id="updateNodeParentId">
        update sys_resource_node SET parent_id = #{parentId}, `node_path` = #{nodePath}, update_time = sysdate() where node_id = #{nodeId} and user_id = #{userId} and del_flag = '0'
    </update>

    <!--    通过主键删除-->
    <update id="deleteById">
        update sys_resource_node
        set del_flag = '2'
        where user_id = #{userId}
          and node_id = #{nodeId}
    </update>
    <!--    通过主键删除-->
    <update id="deleteByIds">
        update sys_resource_node
        set del_flag = '2'
        where user_id = #{userId}
          and node_id in
        <foreach collection="nodeIds" item="nodeId" open="(" separator="," close=")">
            #{nodeId}
        </foreach>
    </update>

    <!--查询所有已删除的子孙节点（含自身），用于恢复时查找-->
    <select id="selectAllChildrenByNodeIdIncludeDeleted" resultMap="SysResourceNodeMap">
        SELECT * FROM sys_resource_node
        WHERE (node_path LIKE CONCAT('%/', #{nodeId}, '/%') OR node_id = #{nodeId})
          AND del_flag = '2'
    </select>

    <!--批量恢复-->
    <update id="restoreByIds">
        update sys_resource_node
        set del_flag = '0'
        where user_id = #{userId}
          and node_id in
        <foreach collection="nodeIds" item="nodeId" open="(" separator="," close=")">
            #{nodeId}
        </foreach>
    </update>
</mapper>

