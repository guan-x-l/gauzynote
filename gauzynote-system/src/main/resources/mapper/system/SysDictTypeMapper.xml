<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.gauzynote.system.mapper.SysDictTypeMapper">

    <resultMap id="SysDictTypeMap" type="com.gauzynote.system.domain.entity.SysDictType">
        <id property="dictId" column="dict_id" jdbcType="BIGINT"/>
        <result property="dictName" column="dict_name" jdbcType="VARCHAR"/>
        <result property="dictType" column="dict_type" jdbcType="VARCHAR"/>
        <result property="userId" column="user_id" jdbcType="BIGINT"/>
        <result property="status" column="status" jdbcType="VARCHAR"/>
        <result property="createBy" column="create_by" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateBy" column="update_by" jdbcType="VARCHAR"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
        <result property="remark" column="remark" jdbcType="VARCHAR"/>
    </resultMap>

    <sql id="selectSysDictTypeVo">
        select dict_id, dict_name, dict_type, user_id, status, create_by, create_time, update_by, update_time, remark
        from sys_dict_type
    </sql>

    <select id="selectDictTypeList" resultMap="SysDictTypeMap">
        <include refid="selectSysDictTypeVo"/>
        <where>
            and (user_id is null or user_id = #{currentUserId})
            <if test="dictType != null and dictType.dictName != null and dictType.dictName != ''">
                and dict_name like concat('%', #{dictType.dictName}, '%')
            </if>
            <if test="dictType != null and dictType.dictType != null and dictType.dictType != ''">
                and dict_type like concat('%', #{dictType.dictType}, '%')
            </if>
            <if test="dictType != null and dictType.status != null and dictType.status != ''">
                and status = #{dictType.status}
            </if>
            <if test="dictType != null and dictType.userId != null">
                and user_id = #{dictType.userId}
            </if>
        </where>
        order by dict_id desc
    </select>

    <select id="selectDictTypeAll" resultMap="SysDictTypeMap">
        <include refid="selectSysDictTypeVo"/>
        where status = '0'
          and (user_id is null or user_id = #{currentUserId})
        order by dict_id desc
    </select>

    <select id="selectDictTypeById" resultMap="SysDictTypeMap">
        <include refid="selectSysDictTypeVo"/>
        where dict_id = #{dictId}
          and (user_id is null or user_id = #{currentUserId})
        limit 1
    </select>

    <select id="selectDictTypeByType" resultMap="SysDictTypeMap">
        <include refid="selectSysDictTypeVo"/>
        where dict_type = #{dictType}
          and (user_id is null or user_id = #{currentUserId})
        limit 1
    </select>

    <select id="selectDictTypeByTypeAndUserId" resultMap="SysDictTypeMap">
        <include refid="selectSysDictTypeVo"/>
        where dict_type = #{dictType}
          and ((#{userId} is null and user_id is null) or user_id = #{userId})
        limit 1
    </select>

    <insert id="insertDictType" parameterType="com.gauzynote.system.domain.entity.SysDictType" useGeneratedKeys="true" keyProperty="dictId">
        insert into sys_dict_type (
            dict_name, dict_type, user_id, status, create_by, create_time, remark
        ) values (
            #{dictName}, #{dictType}, #{userId}, #{status}, #{createBy}, sysdate(), #{remark}
        )
    </insert>

    <update id="updateDictType">
        update sys_dict_type
        <set>
            <if test="dictType.dictName != null and dictType.dictName != ''">dict_name = #{dictType.dictName},</if>
            <if test="dictType.dictType != null and dictType.dictType != ''">dict_type = #{dictType.dictType},</if>
            <if test="dictType.userId != null">user_id = #{dictType.userId},</if>
            <if test="dictType.userId == null">user_id = null,</if>
            <if test="dictType.status != null and dictType.status != ''">status = #{dictType.status},</if>
            <if test="dictType.updateBy != null and dictType.updateBy != ''">update_by = #{dictType.updateBy},</if>
            <if test="dictType.remark != null">remark = #{dictType.remark},</if>
            update_time = sysdate()
        </set>
        where dict_id = #{dictType.dictId}
        <choose>
            <when test="isAdmin">
                and (user_id is null or user_id = #{currentUserId})
            </when>
            <otherwise>
                and user_id = #{currentUserId}
            </otherwise>
        </choose>
    </update>

    <delete id="deleteDictTypeById">
        delete from sys_dict_type
        where dict_id = #{dictId}
        <choose>
            <when test="isAdmin">
                and (user_id is null or user_id = #{currentUserId})
            </when>
            <otherwise>
                and user_id = #{currentUserId}
            </otherwise>
        </choose>
    </delete>
</mapper>
